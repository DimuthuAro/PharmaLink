fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.2
numpy>=1.26.0
joblib>=1.3.2
scikit-learn>=1.8.0
python-multipart>=0.0.6
httpx>=0.25.2

# Image Processing
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# OCR Engines (Pre-trained models)
easyocr>=1.7.0

# YOLOv8 for ROI Detection (lightweight, 3.2M params)
ultralytics>=8.0.200

# Hugging Face Transformers - OCR & NER models
transformers>=4.36.0
torch>=2.1.0
torchvision>=0.16.0
sentencepiece>=0.1.99
accelerate>=0.25.0  # For model loading optimizations

# LLM Refinement (API mode)
openai>=1.6.0

# Optional: Local LLM fallback (4-bit quantization)
# bitsandbytes>=0.41.0  # Uncomment for local Phi-3 model (~1GB VRAM)

# Medical NER (ONNX Runtime for optimized inference)
onnxruntime>=1.16.0

# Model Management
requests>=2.31.0  # For downloading models from URLs

# Data Processing & Training
pandas>=2.0.0
openpyxl>=3.1.0  # For reading Excel files
kaggle>=1.5.16  # For dataset downloads
rapidfuzz>=3.0.0  # For fuzzy matching
matplotlib>=3.7.0  # For visualization (optional)

# Optional: PaddleOCR (uncomment if needed - requires paddlepaddle)
# paddlepaddle>=2.5.0
# paddleocr>=2.7.0

# Optional: Tesseract wrapper
# pytesseract>=0.3.10
